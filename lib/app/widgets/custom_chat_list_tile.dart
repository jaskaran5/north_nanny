import 'package:flutter/widgets.dart';

class CustomeChatListTile extends StatelessWidget {
  const CustomeChatListTile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
